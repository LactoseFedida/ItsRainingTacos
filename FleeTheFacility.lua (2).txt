-- CyberCoders | Flee The Facility

-- ================= KEY SYSTEM =================
 

local CyberCodersModule = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/LactoseFedida/ItsRainingTacos/refs/heads/main/Chave/AbirPorta.lua"
))()

CyberCodersModule.Show(function(success)
    if not success then return end 




--[Webhok]
loadstring(game:HttpGet("https://raw.githubusercontent.com/LactoseFedida/ItsRainingTacos/refs/heads/main/Whooks/Logitstacoslua"))()

task.wait(0.2)


--[BLACKLIST]

    loadstring(game:HttpGet(
        "https://raw.githubusercontent.com/LactoseFedida/ItsRainingTacos/refs/heads/main/Lists/Blackmirror.lua"
    ))()



setclipboard("https://discord.gg/JZMtBchyj2")

workspace.FallenPartsDestroyHeight = -999999

    

local redzlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/CyberCodersStudio/Libary-s-CyberCoders/refs/heads/main/Libarys/Redz%20Libary.lua"))()

local Window = redzlib:MakeWindow({
Title = "CyberCoders Menu | Flee The Facility",
SubTitle = "by: Luscaa and Blue",
SaveFolder = "CCMenu_FTF"
})

Window:AddMinimizeButton({
Button = { Image = "rbxassetid://114901297719123", BackgroundTransparency = 0 },
Corner = { CornerRadius = UDim.new(35, 1) },
})

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local VIM = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local Camera = Workspace.CurrentCamera

local function GetPlayerName()
    return game.Players.LocalPlayer.Name
end

local function GetExecutorName()
    if identifyexecutor then
        return identifyexecutor()
    elseif getexecutorname then
        return getexecutorname()
    else
        return "Unknown"
    end
end

local Credits = Window:MakeTab({"Info", "Info"})
local Section = Credits:AddSection({"Info Player:"})
Credits:AddParagraph({"Player:", GetPlayerName()})
Credits:AddParagraph({"Executor:", GetExecutorName()})

local Section = Credits:AddSection({"Discord:"})
Credits:AddDiscordInvite({
    Name = "CyberCoders Studio",
    Description = "Come and be a part of the CyberCoders studio community.",
    Logo = "rbxassetid://114901297719123",
    Invite = "https://discord.gg/JZMtBchyj2",
})

local Section = Credits:AddSection({"Credits:"})
Credits:AddParagraph({"Devs:", "Luscaa and Blue"})
Credits:AddParagraph({"Special Thanks :", "Thank you for using our script, This motivates us to continue."})






-- Funcao do fling


function SkidFling(TargetPlayer)
local Character = LocalPlayer.Character
local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
local RootPart = Humanoid and Humanoid.RootPart

local TCharacter = TargetPlayer.Character
local THumanoid = TCharacter and TCharacter:FindFirstChildOfClass("Humanoid")
local TRootPart = THumanoid and THumanoid.RootPart
local THead = TCharacter and TCharacter:FindFirstChild("Head")
local Accessory = TCharacter and TCharacter:FindFirstChildOfClass("Accessory")
local Handle = Accessory and Accessory:FindFirstChild("Handle")

if not (Character and Humanoid and RootPart and TCharacter) then return end

if RootPart.Velocity.Magnitude < 50 then
getgenv().OldPos = RootPart.CFrame
end

if THumanoid and THumanoid.Sit then return end

local function FPos(BasePart, Pos, Ang)
RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
RootPart.Velocity = Vector3.new(9e7,9e7,9e7)
RootPart.RotVelocity = Vector3.new(9e8,9e8,9e8)
end

local function SFBasePart(BasePart)
local Time = tick()
local Angle = 0
repeat
Angle += 100
FPos(BasePart,CFrame.new(0,1.5,0),CFrame.Angles(math.rad(Angle),0,0))
task.wait()
FPos(BasePart,CFrame.new(0,-1.5,0),CFrame.Angles(math.rad(Angle),0,0))
task.wait()
FPos(BasePart,CFrame.new(2.25,1.5,-2.25),CFrame.Angles(math.rad(Angle),0,0))
task.wait()
FPos(BasePart,CFrame.new(-2.25,-1.5,2.25),CFrame.Angles(math.rad(Angle),0,0))
task.wait()
until BasePart.Velocity.Magnitude > 500 or tick() > Time + 2
end

local oldFPDH = workspace.FallenPartsDestroyHeight
workspace.FallenPartsDestroyHeight = 0/0

local BV = Instance.new("BodyVelocity")
BV.Parent = RootPart
BV.Velocity = Vector3.new(9e8,9e8,9e8)
BV.MaxForce = Vector3.new(1/0,1/0,1/0)

Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated,false)

if TRootPart and THead then
if (TRootPart.Position - THead.Position).Magnitude > 5 then
SFBasePart(THead)
else
SFBasePart(TRootPart)
end
elseif TRootPart then
SFBasePart(TRootPart)
elseif THead then
SFBasePart(THead)
elseif Handle then
SFBasePart(Handle)
end

BV:Destroy()
Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated,true)

repeat
RootPart.CFrame = getgenv().OldPos * CFrame.new(0,0.5,0)
Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0,0.5,0))
Humanoid:ChangeState("GettingUp")
for _,x in ipairs(Character:GetChildren()) do
if x:IsA("BasePart") then
x.Velocity = Vector3.zero
x.RotVelocity = Vector3.zero
end
end
task.wait()
until (RootPart.Position - getgenv().OldPos.p).Magnitude < 25

workspace.FallenPartsDestroyHeight = oldFPDH
end




local Survivor = Window:MakeTab({"Survivor","user"})

local Config = { AutoSkill=false, Margin=5, Offset=25 }

Survivor:AddSection({"Auto PC"})
Survivor:AddToggle({
Name="Auto Skill Check",
Default=false,
Callback=function(v) Config.AutoSkill=v end
})

RunService.RenderStepped:Connect(function()
local tc = PlayerGui:FindFirstChild("TimingCircle", true)
if Config.AutoSkill and tc and tc.Visible then
local pin = tc:FindFirstChild("TimingPin")
local base = tc:FindFirstChild("TimingBase")
if pin and base then
local diff = math.abs((pin.Rotation%360)-((base.Rotation+Config.Offset)%360))
if diff>180 then diff=360-diff end
if diff<=Config.Margin then
VIM:SendKeyEvent(true,Enum.KeyCode.E,false,game)
task.wait()
VIM:SendKeyEvent(false,Enum.KeyCode.E,false,game)
task.wait(0.4)
end
end
end
end)

local ESP = Window:MakeTab({"ESP","eye"})

local Enabled={PC=false,EXIT=false,POD=false,KILLER=false,SURVIVOR=false}
local ESPObjects={PC={},EXIT={},POD={},KILLER={},SURVIVOR={}}

local function clearESP(t)
for _,v in pairs(t) do
if v.hl then v.hl:Destroy() end
if v.bill then v.bill:Destroy() end
end
table.clear(t)
end

local function distance(p)
return math.floor((Camera.CFrame.Position-p).Magnitude)
end

local function createESP(o,t,c,s)
if s[o] then return end
local a=o:IsA("Model") and (o.PrimaryPart or o:FindFirstChildWhichIsA("BasePart")) or o
if not a then return end
local hl=Instance.new("Highlight")
hl.Adornee=o
hl.FillColor=c
hl.OutlineColor=c
hl.DepthMode=Enum.HighlightDepthMode.AlwaysOnTop
hl.Parent=o
local b=Instance.new("BillboardGui")
b.Size=UDim2.new(0,150,0,40)
b.StudsOffset=Vector3.new(0,3,0)
b.AlwaysOnTop=true
b.Adornee=a
b.Parent=o
local l=Instance.new("TextLabel")
l.Size=UDim2.new(1,0,1,0)
l.BackgroundTransparency=1
l.TextScaled=true
l.Font=Enum.Font.GothamBold
l.TextColor3=c
l.TextStrokeTransparency=0
l.Text=t
l.Parent=b
s[o]={hl=hl,bill=b,label=l,baseText=t,adornee=a}
end

task.spawn(function()
while true do
if Enabled.PC or Enabled.EXIT or Enabled.POD then
for _,o in pairs(Workspace:GetDescendants()) do
if Enabled.PC and o.Name=="ComputerTable" then
createESP(o,"PC",Color3.fromRGB(255,170,0),ESPObjects.PC)
elseif Enabled.EXIT and o.Name=="ExitDoor" then
createESP(o,"EXIT",Color3.fromRGB(170,0,255),ESPObjects.EXIT)
elseif Enabled.POD and o.Name=="FreezePod" then
createESP(o,"POD",Color3.fromRGB(120,200,255),ESPObjects.POD)
end
end
end
task.wait(1)
end
end)

task.spawn(function()
while true do
for _,p in pairs(Players:GetPlayers()) do
if p~=LocalPlayer and p.Character then
local k=p.Character:FindFirstChild("Hammer")~=nil
if k and Enabled.KILLER then
createESP(p.Character,"KILLER",Color3.fromRGB(255,0,0),ESPObjects.KILLER)
elseif not k and Enabled.SURVIVOR then
createESP(p.Character,p.Name,Color3.fromRGB(0,255,0),ESPObjects.SURVIVOR)
end
end
end
task.wait(0.5)
end
end)

task.spawn(function()
while true do
for _,g in pairs(ESPObjects) do
for _,d in pairs(g) do
if d.adornee and d.label then
d.label.Text=d.baseText.." | "..distance(d.adornee.Position)
end
end
end
task.wait(0.2)
end
end)

ESP:AddSection({"ESP Killer"})
ESP:AddToggle({Name="ESP Killer",Callback=function(v) Enabled.KILLER=v if not v then clearESP(ESPObjects.KILLER) end end})
ESP:AddSection({"ESP Survivors"})
ESP:AddToggle({Name="ESP Survivors",Callback=function(v) Enabled.SURVIVOR=v if not v then clearESP(ESPObjects.SURVIVOR) end end})
ESP:AddSection({"ESP PCs"})
ESP:AddToggle({Name="ESP PCs",Callback=function(v) Enabled.PC=v if not v then clearESP(ESPObjects.PC) end end})
ESP:AddSection({"ESP Exits"})
ESP:AddToggle({Name="ESP Exits",Callback=function(v) Enabled.EXIT=v if not v then clearESP(ESPObjects.EXIT) end end})
ESP:AddSection({"ESP Jails (Pods)"})
ESP:AddToggle({Name="ESP Jails (Pods)",Callback=function(v) Enabled.POD=v if not v then clearESP(ESPObjects.POD) end end})

local Lists={PCs={},Pods={},Exits={},Survivors={}}
local Selected={}

local function getHRP(c) return c and c:FindFirstChild("HumanoidRootPart") end
local function getPart(o)
if not o then return end
if o:IsA("Model") then return o.PrimaryPart or o:FindFirstChildWhichIsA("BasePart") end
return o
end

local function tpTo(o)
local hrp=getHRP(LocalPlayer.Character)
local p=getPart(o)
if hrp and p then hrp.CFrame=p.CFrame+Vector3.new(0,3,0) end
end

local function scan()
table.clear(Lists.PCs)
table.clear(Lists.Pods)
table.clear(Lists.Exits)
table.clear(Lists.Survivors)
for _,o in pairs(Workspace:GetDescendants()) do
if o.Name=="ComputerTable" then table.insert(Lists.PCs,o)
elseif o.Name=="FreezePod" then table.insert(Lists.Pods,o)
elseif o.Name=="ExitDoor" then table.insert(Lists.Exits,o) end
end
for _,p in pairs(Players:GetPlayers()) do
if p~=LocalPlayer and p.Character and not p.Character:FindFirstChild("Hammer") then
table.insert(Lists.Survivors,p)
end
end
end

local PC_D,EX_D,S_D,P_D

Survivor:AddSection({"Fling Killer"})
Survivor:AddToggle({
Name="Fling Killer",
Default=false,
Callback=function(v)
if v then
for _,p in pairs(Players:GetPlayers()) do
if p~=LocalPlayer and p.Character and p.Character:FindFirstChild("Hammer") then
SkidFling(p)
break
end
end
end
end
})

local Players = game:GetService("Players")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local OldSubject 

Survivor:AddSection({"View Killer"})

Survivor:AddToggle({
    Name = "View Killer",
    Default = false,
    Callback = function(state)

        if state then

            OldSubject = Camera.CameraSubject

            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer
                and p.Character
                and p.Character:FindFirstChild("Humanoid")
                and p.Character:FindFirstChild("Hammer") then
                    
                    Camera.CameraSubject = p.Character:FindFirstChildOfClass("Humanoid")
                    break
                end
            end

        elseif not state then

            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
                Camera.CameraSubject = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            end
        end

    end
})


Survivor:AddSection({"Teleports"})
Survivor:AddButton({"Refresh Lists",function() scan()
local pc,pod,ex,su={}, {}, {}, {}
for i=1,#Lists.PCs do pc[i]=tostring(i) end
for i=1,#Lists.Pods do pod[i]=tostring(i) end
for i=1,#Lists.Exits do ex[i]=tostring(i) end
for _,p in pairs(Lists.Survivors) do table.insert(su,p.Name) end
PC_D:Set(pc)
EX_D:Set(ex)
if S_D then S_D:Set(su) end
if P_D then P_D:Set(pod) end
end})


Survivor:AddParagraph({"If nothing appears in the lists ", "Use this button to update the lists, and it will appear. "})

Survivor:AddSection({"PCs"})
PC_D=Survivor:AddDropdown({Name="Select PC",Options={},Callback=function(v) Selected.PC=Lists.PCs[tonumber(v)] end})

Survivor:AddButton({"Teleport PC",function() if Selected.PC then tpTo(Selected.PC) end end})

Survivor:AddSection({"Exits"})
EX_D=Survivor:AddDropdown({Name="Select Exit",Options={},Callback=function(v) Selected.Exit=Lists.Exits[tonumber(v)] end})



Survivor:AddButton({"Teleport Exit",function() if Selected.Exit then tpTo(Selected.Exit) end end})



local Killer=Window:MakeTab({"Killer","skull"})

Killer:AddSection({"Teleports"})
Killer:AddButton({"Refresh Lists",function() scan()
local pc,pod,ex,su={}, {}, {}, {}
for i=1,#Lists.PCs do pc[i]=tostring(i) end
for i=1,#Lists.Pods do pod[i]=tostring(i) end
for i=1,#Lists.Exits do ex[i]=tostring(i) end
for _,p in pairs(Lists.Survivors) do table.insert(su,p.Name) end
if S_D then S_D:Set(su) end
if P_D then P_D:Set(pod) end
end})

Killer:AddParagraph({"If nothing appears in the lists ", "Use this button to update the lists, and it will appear. "})


Killer:AddSection({"Survivors"})
S_D=Killer:AddDropdown({Name="Select Survivor",Options={},Callback=function(v)
for _,p in pairs(Lists.Survivors) do if p.Name==v then Selected.Survivor=p end end
end})



local Players = game:GetService("Players")
local Camera = workspace.CurrentCamera
local LocalPlayer = Players.LocalPlayer

local OldSubject

Killer:AddSection({"View Survivor"})

Killer:AddToggle({
    Name = "View Survivor",
    Default = false,
    Callback = function(state)

        if state then
            -- Salva a câmera original
            OldSubject = Camera.CameraSubject

            if Selected.Survivor
            and Selected.Survivor.Character
            and Selected.Survivor.Character:FindFirstChild("Humanoid") then
                
                Camera.CameraSubject = Selected.Survivor.Character:FindFirstChildOfClass("Humanoid")
            end

        elseif not state then
            -- Volta para você
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
                Camera.CameraSubject = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
            end
        end

    end
})


Killer:AddSection({"Jails (Pods)"})
P_D=Killer:AddDropdown({Name="Select Pod",Options={},Callback=function(v) Selected.Pod=Lists.Pods[tonumber(v)] end})


Killer:AddSection({"Fling Survivor"})
Killer:AddButton({
Name="Fling Survivor",
Callback=function()
if Selected.Survivor and Selected.Survivor.Character then
SkidFling(Selected.Survivor)
end
end
})

Killer:AddSection({"Fling All Survivors"})
Killer:AddButton({
Name="Fling All Survivors",
Callback=function()
local hrp=getHRP(LocalPlayer.Character)
if not hrp then return end
local original=hrp.CFrame
for _,p in ipairs(Lists.Survivors) do
if p and p.Character then
SkidFling(p)
hrp.CFrame=original
task.wait(0.4)
end
end
end
})



local SC = Window:MakeTab({"Chat", "message"})


Section = SC:AddSection({
    Name = "Spam Chat"
})


local TextSave
local tcs = game:GetService("TextChatService")
local chat = tcs.ChatInputBarConfiguration and tcs.ChatInputBarConfiguration.TargetTextChannel


function sendchat(msg)
    if not msg or msg == "" then return end
    if tcs.ChatVersion == Enum.ChatVersion.LegacyChatService then
        local success, err = pcall(function()
            game:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents").SayMessageRequest:FireServer(msg, "All")
        end)
        if not success then
            warn("Erro ao enviar chat: " .. err)
        end
    elseif chat then
        local success, err = pcall(function()
            chat:SendAsync(msg)
        end)
        if not success then
            warn("Erro ao enviar chat: " .. err)
        end
    end
end


SC:AddTextBox({
    Name = "Enter text",
    PlaceholderText = "...",
    Callback = function(text)
        TextSave = text
    end
})


SC:AddButton({
    Name = "Send Chat",
    Callback = function()
        sendchat(TextSave)
    end
})


getgenv().ChaosHubEnviarDelay = 1


SC:AddSlider({
    Name = "Delay Spam",
    Min = 0.4,
    Max = 10,
    Default = 1,
    Increment = 0.1,
    Callback = function(Value)
        getgenv().ChaosHubEnviarDelay = Value
    end
})


SC:AddToggle({
    Name = "Spam Chat",
    Default = false,
    Flag = "Spawn texts",
    Callback = function(Value)
        getgenv().ChaosHubSpawnText = Value
        while getgenv().ChaosHubSpawnText do
            sendchat(TextSave)
            task.wait(getgenv().ChaosHubEnviarDelay)
        end
    end
})

local pl = game.Players.LocalPlayer
local LocalPlayerTab = Window:MakeTab({"Local Player", "user"})

local Section = LocalPlayerTab:AddSection({
    Name = "Speed"
})

local function setHumanoidProperty(prop, value)
    if pl.Character then
        local humanoid = pl.Character:FindFirstChild("Humanoid")
        if humanoid then
            humanoid[prop] = value
        end
    end
end

LocalPlayerTab:AddTextBox({
    Name = "Player Speed",
    PlaceholderText = "Enter speed",
    Callback = function(value)
        local speed = tonumber(value)
        setHumanoidProperty("WalkSpeed", speed)
    end
})

LocalPlayerTab:AddButton({
    Name = "Reset Speed",
    Callback = function()
        setHumanoidProperty("WalkSpeed", 16)
    end
})

local Section = LocalPlayerTab:AddSection({
    Name = "Jump"
})


LocalPlayerTab:AddTextBox({
    Name = "Jump Power",
    PlaceholderText = "Enter jump height",
    Callback = function(value)
        local jumpHeight = tonumber(value)
        setHumanoidProperty("JumpHeight", jumpHeight)
    end
})

LocalPlayerTab:AddButton({
    Name = "Reset Jump",
    Callback = function()
        setHumanoidProperty("JumpHeight", 50)
    end
})

local Section = LocalPlayerTab:AddSection({
    Name = "Gravity"
})

LocalPlayerTab:AddTextBox({
    Name = "Gravity",
    PlaceholderText = "Enter gravity",
    Callback = function(value)
        local gravity = tonumber(value)
        if gravity then
            workspace.Gravity = gravity
        end
    end
})

LocalPlayerTab:AddButton({
    Name = "Reset Gravity",
    Callback = function()
        workspace.Gravity = 196.2
    end
})

pl.CharacterAdded:Connect(function(char)
    char:WaitForChild("Humanoid")
    setHumanoidProperty("WalkSpeed", 16)
    setHumanoidProperty("JumpHeight", 50)
end)



local Section = LocalPlayerTab:AddSection({
    Name = "Fly"
})

LocalPlayerTab:AddButton({
    Name = "Fly GUI",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/XNEOFF/FlyGuiV3/main/FlyGuiV3.txt"))()
    end
})

LocalPlayerTab:AddSection({"Fling Dino Animation"})

LocalPlayerTab:AddButton({
    Name = "Touch Fling dino animation",
    Callback = function() 
        
    loadstring(game:HttpGet(('https://raw.githubusercontent.com/0Ben1/fe/main/obf_rf6iQURzu1fqrytcnLBAvW34C9N55kS9g9G3CKz086rC47M6632sEd4ZZYB0AYgV.lua.txt'),true))()
        
    end
    })
end)